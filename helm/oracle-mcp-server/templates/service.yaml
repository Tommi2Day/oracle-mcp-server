apiVersion: v1
kind: Service
metadata:
  name: {{ include "oracle-mcp-server.fullname" . }}
  labels:
    {{- include "oracle-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oracle-mcp-server.selectorLabels" . | nindent 4 }}
